import { TurnBgBlueDirective } from './turn-bg-blue.directive';

describe('TurnBgBlueDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnBgBlueDirective();
    expect(directive).toBeTruthy();
  });
});
