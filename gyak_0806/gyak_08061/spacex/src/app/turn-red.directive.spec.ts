import { TurnRedDirective } from './turn-red.directive';

describe('TurnRedDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnRedDirective();
    expect(directive).toBeTruthy();
  });
});
